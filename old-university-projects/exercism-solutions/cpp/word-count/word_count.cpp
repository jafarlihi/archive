#include "word_count.h"

using std::string;
using std::map;

namespace word_count {
    map<string, int> words(string input) {
        map<string, int> result;

        for (char& c : input) {
            
        }

        return result;
    }
}
